/* JS is LOOSELY typed. 
      What this means is you dont have to define the data type */

/* Js is DYNAMICALLY typed. 
      Which means, whatever the value you typed, data type of variable will change to ot */

var num = 16; // Number
var Name = 'Ramazan Bulbul'; //String
var flag = true; //Boolean

var x; // Declaration
x = 5; // Initilization

/* Naming Conventions - Identifier (The name you gave to a variable    ) */
/* 
        The rules for creating an identifier in JS are, the name of the identifier 
        should not be any pre-defined keyword (like "var,let,const"), the fir char. must be a letter, 
        an uderscore(_),or a dollar sign ($). Subsequent char. may be any letter
        or digit or an underscrore or dollar sign. Variables are case sensitive */

var _N$ame = 'Ramazan Bulbul';

alert(num);

document.write(num); // printing on the browser
document.write(Name);
document.write(flag);
document.write(x);
